var setText = function(item,iconAll){
  if(item.name!=undefined){
    console.log(item.name)
    return item.name
  }
  console.log(iconAll[item.type][item.index].name)
  return iconAll[item.type][item.index].name
}

module.exports = {
  setText: setText
}